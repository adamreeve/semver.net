The semantic versioner for .NET.
================================

This library implements the
[Semantic Versioning specification](http://semver.org/)
and the version ranges used by
npm ([node-semver](https://github.com/npm/node-semver)).
